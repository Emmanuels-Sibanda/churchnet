# Server Configuration
PORT=5000

# JWT Secret - MUST be set to a strong random string in production
# Generate a secure secret using: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your-secret-key-change-in-production

# CORS Configuration
# For production, set this to your frontend domain (e.g., https://yourdomain.com)
# For development, leave as localhost:3000
CORS_ORIGIN=http://localhost:3000
